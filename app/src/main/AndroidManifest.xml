<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.onlinetestexam">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ElectronicSubActivity"></activity>
        <activity android:name=".EeeDashBoard" />
        <activity android:name=".RankingDashBoard" />
        <activity android:name=".QuestionActivity" />
        <activity android:name=".SoftwareSubActivity" />
        <activity android:name=".CseDashBoard" />
        <activity android:name=".PasswordRecovery" />
        <activity android:name=".NavigationDrawer" />
        <activity android:name=".SignUp" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>